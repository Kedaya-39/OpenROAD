
我的毕设题目是《考虑单元库可行性分析的设计制造协同优化》（DTCO with Library Feasibility Analysis）。研究目标是在设计技术协同优化（DTCO）流程中实现对标准单元库的自动化可行性分析，通过优化标准单元的选择和使用策略，推动更高效的数字电路设计实现。本课题选择将研究重点聚焦在引脚可访问性分析上。

本课题的详细技术细节为：

相关概念:
拆线重布（Rip-up and Reroute）：布线算法中用于解决布线资源冲突的一种迭代策略。在当前信号线无法找到合法路径时，布线工具会暂时移除部分已布好的、造成阻塞的信号线，然后重新规划所有受影响信号线的路径，以期找到全局可行的布线方案。
布线轨道（Routing Track）： 布线轨道是物理版图设计中基于特定金属层的设计规则定义的虚拟网格线。它们定义了金属互连线的中心在特定金属层上所允许放置的位置，是布线算法运行的基本网格。
引脚接入点（Pin Access Point）： 标准单元引脚几何图形上预先定义的、符合设计规则的特定坐标位置。布线工具必须通过通孔（Via）或金属线精确连接到这些点上，以实现电气导通。一个引脚可能有多个接入点。
独特单元实例（Unique Instance）：这是一种用于高效分析引脚可访问性的标准单元实例分类方法。两个单元实例若以下三个特征完全相同，则被视为同一种独特单元实例：
(i) 该单元实例对应的标准单元；
(ii) 放置方向，即单元实例是否发生了旋转或翻转；
(iii) 轨道偏移量，即单元实例相对于布线轨道的位置偏移。算法仅需针对每种独特单元实例进行分析，即可覆盖所有同类实例，从而显著减少降低计算复杂度。

接入模式（Access Pattern）： 针对一种独特单元实例，接入模式是指为其每个引脚选定一个接入点所构成的组合方案。该方案必须确保所有被选中的接入点之间满足设计规则（如间距要求），且彼此兼容，使得布线工具能够同时对它们进行连接。一种独特单元实例可能存在多个可行的接入模式。
引脚可访问性（Pin Accessibility）：在满足设计规则约束的前提下，标准单元的输入、输出端口可被布线工具成功连接的难易程度。

一、本课题要解决的技术问题：
在先进工艺节点的数字集成电路设计中，设计制造协同优化已成为实现芯片良率与性能目标的关键方法论。标准单元库作为连接工艺制造与电路设计的核心桥梁，其设计质量直接决定了最终芯片的性能、功耗和面积（PPA）上限。然而，现有的技术机制缺乏对标准单元在实际电路设计中的可行性，特别是引脚可访问性的自动化、系统性评估机制。
具体而言，现有技术存在以下问题：
1. 评估滞后且依赖人工经验，没有实现自动化
在标准单元驱动的DTCO流程中，现有的研究旨在加速标准单元的特征化过程。然而，一个标准单元纸面参数的优良，并不代表其在具体设计的复杂布局布线环境中易于实现连接或能发挥预期效果。目前，判断一个标准单元是否“好用”（即可行性高），依赖于工程师在完成布局布线后，通过分析电子设计自动化（EDA）工具产生的报告（如布线拥塞报告、时序违例报告等）进行定性判断。这种后验式的方法不仅耗时，而且难以自动化、不可复用。
2. 缺乏对引脚可访问性的系统性评价标准
随着工艺微缩，设计规则日益复杂且图形密度增加，标准单元的引脚可访问性已成为制约布线成功率、影响时序收敛的关键因素。现有技术缺乏一套系统性的量化评价标准，难以有效匹配布线资源与需求，限制了布线质量的提升。
因此，本课题旨在解决上述问题，提供一套可集成于芯片物理实现流程中的标准单元引脚可访问性量化评价方法及系统。通过构建系统性的评价指标与自动化反馈机制，优化标准单元的选择与使用策略，从而减少迭代次数、缩短设计周期，并完成DTCO的反馈闭环。

二、本发明创造的技术方案： 
1. 工作原理
本课题的核心在于构建一个标准单元引脚可访问性自动化评价与反馈系统。该系统嵌入在数字电路设计实现流程中，并可作为DTCO的反馈环节。其工作过程遵循“静态特征提取-动态监控-综合评估-反馈指导”的闭环逻辑。
系统首先从标准单元库及工艺库文件中提取单元的几何信息，为每种独特单元实例生成所有可行的接入模式，并计算其静态评估评分。在电路设计实现过程中，系统动态收集与引脚可访问性相关的动态评估数据。
本课题建立了双层级量化评分体系：
- 接入模式评分：针对每个独特单元实例的每一个接入模式，综合其静态指标与动态监控数据，计算得到一个实时更新的评分。该评分直接用于指导布线工具在当前实例的多个接入模式中做出最优选择。
- 标准单元评分：基于标准单元对应所有接入模式的评分，以及该标准单元接入模式的整体容量与多样性，计算得到对该标准单元的综合评分。该评分用于在设计优化环节指导EDA工具进行单元选择，并提出对标准单元标记“慎用”或“禁止使用”的建议，还可以为标准单元库开发提供优化反馈。
以下详细介绍评价指标的计算方法及双层级评分体系的构建。所有指标值执行归一化处理后再加权求和计算上述两级评分。

A. 接入模式静态评价指标
指标1 - 布线轨道对齐度（I_1）
- 定义：评估接入点中心是否与金属层的布线轨道对齐。未对齐将增加布线难度。
- 计算方式：遍历接入模式中的每个接入点ap，在X、Y轴方向分别计算：若ap坐标与轨道中心完全对齐，得s_11分；若ap位于轨道中心，或引脚形状的中心，得s_12分；否则得0分。s_11>s_12，接入点得分i_ap^1∈[0,2s_11]。接入模式得分I_1为所有接入点得分的算数平均值。

指标2 - 可接入方向（I_2）
- 定义：量化接入点的进线灵活性。分析每个接入点可以被布线线段连接的有效方向（如东、南、西、北、上）
- 计算方式：遍历接入模式中的每个接入点ap：若ap支持通孔接入，则加s_21分；平面可接入方向（东、南、西、北）每个加s_22分。s_21>s_22，接入点得分i_ap^2∈(0,s_21+4s_22]。接入模式得分I_2为所有接入点得分的算数平均值。

指标3 - 空间稀疏性（I_3）
- 定义：量化接入模式所选接入点在单元版图空间中分布的稀疏与均匀程度。过于密集可能导致局部布线拥塞。
- 计算方式：设接入模式包含M个接入点，每个接入点ap_i的坐标为(x_i,y_i,l_i ),i∈[1,m],l_i为从下到上的金属层序号。首先计算所有接入点的质心c坐标(x_i,y_i,l_i )=(1/M \sum_{i=1}^M x_i, 1/M \sum_{i=1}^M y_i, 1/M \sum_{i=1}^M l_i)，然后计算所有接入点坐标相对于质心c的方差 V=1/M \sum_{i=1}^M [(x_i-x_c)^2+(y_i-y_c)^2+(l_i-l_c)^2]，则I_3=V/Area_cell，其中Area_cell为标准单元的面积。

指标4 - 占用轨道数（I_4）
- 定义：统计接入模式占用的布线轨道数量，作为对消耗的布线资源的估计。
- 计算方式：I_4=1-N_occ/N_cell 。其中N_occ为接入模式所覆盖或阻断的水平及垂直方向布线轨道总数，N_cell为该标准单元边界范围内可用的布线轨道总数。

接入模式的静态评分由上述指标加权求和得到：S_{pattern}^{static}=\sum_{k=1}^4 w_k \cdot Norm(I_k)，其中w_k为权重，Norm为归一化函数。

B. 标准单元静态评价指标：
指标5 - 接入模式集容量（I_5）
- 定义：评估标准单元所能提供的有效接入模式数量，反映其解空间大小。
- 计算方式：I_5=\begin{cases} s_{51}\cdot N/N_{th}, & N \leq N_{th} \\ s_{51}+s_{52}\cdot \ln⁡(1+N/N_{th}), & N>N_{th} \end{cases}，其中N为标准单元的接入模式的数量，N_th,s_51,s_52为常量。

指标6 - 接入模式多样性（I_6）
- 定义：评估标准单元所能提供的接入模式的丰富程度。不仅要求尽可能多的覆盖接入点，还要求接入模式对接入点的选择尽可能不同。
- 计算方式：令标准单元（独特单元实例）的接入点集合为AP_total；令标准单元的接入模式集合为PATTERN={pattern_1,pattern_2,\cdots,pattern_N}，N为接入模式的总数；对 \forall pattern_i\in PATTERN，令AP(pattern_i )\subseteq AP_total表示该接入模式的接入点集合。
则接入点覆盖率为：I_cov= |⋃_{k=1}^N AP(pattern_k)| / |AP_total|，
平均Jaccard差异系数为:
I_{jac}=\begin{cases} 0, & N<2 \\ 2/N(N-1) \sum_{1≤i<j≤N} (1-|AP(pattern_i)\cap AP(pattern_j)| / |AP(pattern_i)\cup AP(pattern_j)|), & N\beq 2 \end{cases}。
则  I_6= s_61⋅I_cov+s_62⋅I_jac，其中s_61和s_62为常数。

设标准单元有N个接入模式，则标准单元的静态评分定义为：S_cell^static=1/N \sum_{i=1}^N S_{pattern_i}^{static} + \sum_{k=5}^6 w_k\cdot Norm(I_k)。

C. 动态评估段指标
指标7 - 全局布线结果遵循度（Guide Adherence）（I_7）
- 定义：检查是否遵循了全局布线的规划路径。严重偏离意味着详细布线可能失败。
- 计算方式：若接入模式的接入点全部位于全局布线的规划路径范围内，得1分；否则得0分。

指标8 - 线长估计（I_8）
- 定义：估算从接入点到其所连接线网中心的曼哈顿距离。距离越大通常预示着更长的连线。
- 计算方式：设接入模式包含M个接入点，I_8=1/M sum_{i=1}^M Dist_{Manh}(ap_i,net_i.center)，其中Dist_{Manh}为两点之间的曼哈顿距离，net_i.center为接入点所连接的线网中心点。

指标9 - 邻域相容性（Neighbor Compatibility）（I_9）
- 定义：记录当该单元与其他类型的单元相邻摆放时，引脚访问相容的累计次数（例如：两个单元的引脚靠得太近，导致无法同时出线）。
- 计算方式：在以单元实例为中心的矩形窗口W内，遍历所有其他单元实例。I_9=N_com/N_all ，其中N_com为与当前接入模式无DRC冲突的相邻单元实例的接入模式数量，N_all为相邻单元实例的接入模式总数。

指标10 - 拆线重布触发次数（I_10）
- 定义：记录布线环节该接入模式涉及的拆线重布的累计次数。这是引脚可访问性差的直接体现。
- 计算方式： I_10=N_{selected}-s_{101} N_{ripup}-\sum_{j\in Ripups_W}s_{102}/Dist(net_j, c)，其中N_{selected}为该接入模式被选用的次数，N_{ripup}为该接入模式被拆线的次数，W为以单元实例为中心的矩形窗口，Ripups_W为W内的拆线事件，net_j为事件j涉及的线网，c为接入模式的质心，s_{101}和s_{102}为常数。

综合考虑上述指标，接入模式的动态评分为：S_{pattern}^{dynamic}=\sum_{k=7}^10 w_k \cdot Norm(I_k)，其中w_k为权重，Norm为归一化函数。
则接入模式的评分定义为：S_pattern=S_pattern^static+S_pattern^dynamic。
标准单元的动态评分为：S_cell^dynamic=1/N \sum_{i=1}^N S_{pattern_i}^{dynamic} 。
则标准单元的评分定义为：S_cell=S_cell^static+S_cell^dynamic。

2. 产品结构
/*
 * =====================================================================================
 *
 * [Target Design]      [Design Constraints]      [Std Cell Library]
 * |                      |                        |
 * +----------------------+------------------------+
 *             |
 * +-----------v------------+---------------------------------------------------------+
 * |  PHYSICAL DESIGN FLOW  |               PROPOSED SOFTWARE SYSTEM                  |
 * |           |            |                                                         |
 * |      [Floorplan] ----->|--->[Access Pattern Gen.]----->[Static Feature Ext]---+  |
 * |           |            |              |                                       |  |
 * |           v            |              |                                       |  |
 * |      [Placement] ----->|------------------------------>[Dynamic Feature Col]  |  |
 * |           |            |              |                           |           |  |
 * |           v            |              |                           v           |  |
 * |         [CTS]          |              |                 [Core Calculation] <--+  |
 * |           |            |              |                           |              |
 * |           v            |              v                           |              |
 * |       [Routing] <------|<--[Unique Cell Inst.]<--(Pattern Score)--+              |
 * |           |            |   [ & Access Pattern]                    |              |
 * |           v            |                                          v              |
 * |       [Signoff] <------|<--------------------------------[Std Cell Scoring]      |
 * |           |            |                                          |              |
 * |           v            |                                          v              |
 * |        [GDSII]         |                                [Std Cell Evaluation]    |
 * +------------------------+---------------------------------------------------------+
 * =====================================================================================
 */

本课题实现可视为一个软件系统或集成于现有EDA工具中的功能模块。该系统包含以下核心组件，协同工作以实现对引脚可访问性的自动化评价与反馈：
- 接入模式生成模块（模块1）：负责解析标准单元库的物理库文件与工艺设计规则，基于输入的单元类型、放置方向及布线轨道偏移量，为每一种独特单元实例枚举并生成所有符合设计规则约束的接入点及接入模式。
- 静态特征提取模块（模块2）：与接入模式生成模块联动，针对生成的每一个接入模式，依据预设的静态评价指标，计算生成模式和标准单元的静态评分（S_pattern^static和S_cell^static）。
- 动态特征收集模块（模块3）：作为物理设计实现流程的监控器，集成于布局、布线等关键阶段。该模块负责从当前设计上下文中实时收集与引脚可访问性相关的动态数据，如全局布线路径遵循情况、邻域单元信息、拆线重布事件等，并提供给核心计算模块。
- 核心计算模块（模块4）：该模块是系统的核心。它接收来自静态特征提取模块和动态特征收集模块的数据，内置双层级评分算法。负责执行数据的归一化处理、权重分配，并计算接入模式评分S_pattern及标准单元评分S_cell。
另外，本系统包含以下两个数据库：
- 独特单元实例及接入模式库：存储生成的独特单元实例、对应的接入模式集合及其评分。该数据库向物理设计流程的布线环节提供接口，使布线工具能够读取并优先选用高评分的接入模式。
- 标准单元评分库：存储标准单元的指标数据及评分。该数据库可用于在设计优化环节指导EDA工具进行单元选择，提出对标准单元的使用建议，还可以为标准单元库开发提供优化反馈。

上述模块按序协作。首先由模块1、2完前期数据准备工作。在设计流程中，模块3持续收集动态数据并触发模块4进行在线评分计算，计算结果同步至数据库。数据库向外提供相应功能接口，完成闭环。

三、本课题的效果和优点
本课题构建了涵盖多维特征的引脚可访问性系统性量化评价标准，填补了现有技术空白；基于此建立的自动化评估流程，不仅能向数字实现各环节引擎提供标准单元的决策支持，显著减少详细布线迭代、提升布线质量并加速设计收敛，还能向工艺库开发端提供基于真实设计场景的反馈，指导标准单元版图的针对性优化，从而实现了实质性的、数据驱动的设计制造协同优化（DTCO）闭环。

我计划在开源eda工具openroad基础上，实现我的想法。